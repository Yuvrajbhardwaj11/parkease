{
  "name": "parkease",
  "version": "1.0.0",
  "private": true,
  "description": "ParkEase — monorepo root. Holds only the deps the /api serverless function needs; the client has its own package.json.",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.2"
  }
}
